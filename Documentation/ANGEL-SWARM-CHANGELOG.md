# ANGEL SWARM — Changelog (duplicate copy)

UNCLASSIFIED // SYNTHETIC DATA // FOR DEMONSTRATION ONLY

`Documentation/ANGEL-SWARM-CHANGELOG.txt` is a byte-for-byte duplicate of the changelog at the repository root — verified with `diff`, which reports no difference between the two files. It is kept here because the written deliverables in this folder refer to the changelog alongside them, and the root copy is the one the package has always shipped standalone.

Rather than render the same 2,979 lines twice, the Markdown rendering lives at the root:

**[../ANGEL-SWARM-CHANGELOG.md](../ANGEL-SWARM-CHANGELOG.md)**

Both plain-text copies remain in place and are authoritative; the Markdown is a rendering of them.
